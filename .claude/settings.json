{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /tmp/cc-agent/62781746/project && npm run build 2>&1 | tail -5 | { output=$(cat); if echo \"$output\" | grep -q 'error\\|Error\\|Failed'; then echo \"{\\\"systemMessage\\\": \\\"Build failed:\\\\n$output\\\"}\"; else echo '{}'; fi }",
            "timeout": 120,
            "statusMessage": "Running npm run build...",
            "asyncRewake": true,
            "rewakeSummary": "Stop hook feedback",
            "rewakeMessage": "You did not run \"npm run build\". Run \"npm run build\" to ensure the project builds correctly, and fix any issues."
          }
        ]
      }
    ]
  }
}
